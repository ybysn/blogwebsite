# SelfBlogWeb

博客 blog.ybysn.org — Next.js 16 + MDX + Tailwind CSS 4。

## 技术栈

- Next.js 16 (App Router)
- MDX (文章用 .mdx)
- Tailwind CSS 4
- Giscus 评论
- RSS (scripts/generate-rss.ts)
- Cloudflare Pages 部署

## 目录结构

```
app/          → 路由 (layout, page, posts/[slug], search, about, tools, tutorial)
components/   → 共享组件
content/      → MDX 文章 (posts/ 子目录)
lib/          → 工具函数 (posts.ts, mdx.ts)
public/       → 静态资源 (images/, 配图)
scripts/      → 构建脚本 (RSS, workers)
docs/         → 项目文档
```

## 博客核心规则

### 配图
- 浅色主题: SVG 背景 #fafafa
- 深色主题: `-dark.svg` 后缀, 背景 #020617
- 使用 `<ThemedImage>` 组件 (来自 components/)

### 色板
- cyan, emerald, amber, violet, rose

### 发布
- **匿名化**: 文中不得出现真实域名、用户名、IP
- 发布后运行 `npm run build` → `wrangler pages deploy`

## 开发规则

- 每完成一个功能/修复 → git commit + push
- 改代码前先看已有组件和样式, 复用现有模式
- 配图生成用 AI (ImageGenerate), 至少出浅色版
- 文章 slug 用英文短横线命名, 中文标题在 frontmatter

## 常用命令

```bash
npm run dev      # 开发服务器
npm run build    # 构建
npm run rss      # 生成 RSS
```
